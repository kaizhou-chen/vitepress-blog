## 添加其他依赖

```bash
# 常用第三方库
npm install vue-router element-plus @element-plus/icons-vue
npm install axios qs pinia mitt dayjs lodash-es

npm install --save-dev less less-loader  @types/qs @types/lodash-es

# 其他第三方库
npm install echarts vuedraggable

npm install --save-dev @types/sortablejs
```