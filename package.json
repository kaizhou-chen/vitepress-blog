{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",

    "test": "node src/index.js",
    "test:build": "esbuild src/index.js --bundle --minify --outfile=src/dist/dist.js --platform=node"
  },
  "dependencies": {
    "esbuild": "^0.23.1"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "vitepress": "^1.3.4"
  }
}
