{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    
    "test": "node test/index.js",
    "test:build": "esbuild test/index.js --bundle --minify --outfile=test/dist/dist.js --platform=node"
  },
  "dependencies": {
    "esbuild": "^0.23.1"
  },
  "devDependencies": {
    "vitepress": "^1.3.4"
  }
}
